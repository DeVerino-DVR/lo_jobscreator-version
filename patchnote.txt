<patchnote>
1.0.2
- Fix: item creator — `lo_item_usables` is now only written when the item is both Usable AND Consumable (otherwise the row is removed on create and update), preventing stray inserts for plain items
</patchnote>
